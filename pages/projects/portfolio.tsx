import { NextPage } from 'next';
import { Shell } from '../../components/Shell';

const Portfolio: NextPage = () => {
  return (
    <Shell>
      <div className="w-full">Portfolio</div>
    </Shell>
  );
};

export default Portfolio;
